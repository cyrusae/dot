[project]
name = "dot-poc"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "apscheduler>=3.10",
    "discord.py>=2.3",
    "mcp",
    "pyyaml",
]

[project.scripts]
dot = "dot_cli:main"
dot-coordinator = "orchestrator.app:main"
